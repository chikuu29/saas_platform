apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - pvc.yaml
  - sealed-secret.yaml          # MongoDB credentials SealedSecret only
  - configmap-init.yaml
  - statefulset.yaml
  - service.yaml
