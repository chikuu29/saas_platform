apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Identity database tier — PostgreSQL + Redis.
# Both databases deploy into the "identity" namespace (created by saas-platform-infra/namespaces/).
# Apply order: postgres first (primary store), redis second (cache/session store).
resources:
  - postgres/
  - redis/
