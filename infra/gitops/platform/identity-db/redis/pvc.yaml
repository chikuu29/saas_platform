apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: identity-redis-pvc
  namespace: identity
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
