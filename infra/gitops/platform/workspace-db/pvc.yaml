apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: workspace-mongodb-pvc
  namespace: workspace
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
