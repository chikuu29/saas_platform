apiVersion: v1
kind: Service
metadata:
  name: workspace-web
  namespace: workspace
spec:
  type: ClusterIP
  selector:
    app: workspace-web
  ports:
    - port: 80
      targetPort: 80
      name: http
